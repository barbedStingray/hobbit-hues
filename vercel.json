
{
  "version": 2,
  "builds": [
    {
      "src": "server/*/.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/server/routes/(.*)",
      "dest": "server/api/$1"
    },
    {
      "src": "/(.)",
      "dest": "public/$1"
    }
  ]
}
